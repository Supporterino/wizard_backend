import { Deck } from './classes/deck'

const drawing_pile: Deck = new Deck();

console.log(drawing_pile.toString());
// drawing_pile.getCards().forEach(element => {
//     console.log(element.toString());
// });